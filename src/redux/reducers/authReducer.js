const initState = {
    isLoggin: false,
    token: null
}

const authReducer = (state = initState, action) => {
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}

export default authReducer
const initState = {
    userData: {}
}

const userReducer = (state = initState, action) => {
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}

export default userReducer